import React from 'react'

const UsingProps = (props) => {
  return (
    <div>
      {/* First component of using props */}
      <h1> Hello {props.name}</h1>
    </div>
  )
}

export default UsingProps
